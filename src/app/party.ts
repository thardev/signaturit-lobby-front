export class Party {

  name: string;
  points: number;
  contract: string;

  constructor(name: string, points: number, contract: string) {
    this.name = name;
    this.points = points;
    this.contract = contract;
  }
}
